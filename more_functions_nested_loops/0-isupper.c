#include "main.h"
#include <stdio.h>
#include <ctype.h>

/*
 * _isupper  - return if C upper
 * Return c
 * @x data add
 *
 */

int _isupper(void)
{
	char x = 'C';

	if (isupper(x))
	{
	return (1);
	}
	return (0);
}
